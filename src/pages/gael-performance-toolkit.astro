---
// The toolkit hub moved under /content-hub/ so every series lives in one
// place. This redirect keeps older links, bookmarks and any shared URLs
// working rather than 404ing.
export const prerender = false;

return Astro.redirect('/content-hub/gael-performance-toolkit/', 301);
---

---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`Performance Plans | ${SITE_TITLE}`} description="Performance plans for female athletes. Physical training plans + mental performance plans, all delivered online with a special focus on women's health." />
  </head>
  <body class="personal-training-page">
    <Header />
    <main class="container">
<section class="intro">

  <div class="pt-band pt-band-intro">
    <div class="pt-band-inner hero-section">
 <h1>
  Performance Plans Built For Female Athletes
</h1>

<p class="hero-copy">
  Personalised mental and physical performance programmes
  designed around your sport, goals, and women's health.
</p>

<div class="benefits-grid">
  <div class="benefit-card benefit-card--lime">
    <h3>Built for Female Athletes</h3>
    <p>Every plan considers the realities of women's health, training load, and long-term performance.</p>
  </div>
  <div class="benefit-card benefit-card--lime">
    <h3>Tailored to You</h3>
    <p>Your sport, goals, equipment, experience, and schedule shape the programme.</p>
  </div>
  <div class="benefit-card benefit-card--purple">
    <h3>Evidence-Based</h3>
    <p>Practical recommendations grounded in current research and coaching experience.</p>
  </div>
  <div class="benefit-card benefit-card--purple">
    <h3>Self-Paced</h3>
    <p>Receive your complete programme and work through it on your own schedule.</p>
  </div>
</div>

<section class="results-section">

  <h2>Trusted By Female Athletes</h2>

  <div class="results-grid">

    <div class="result-card">
      ⭐⭐⭐⭐⭐
      <p>
        "The confidence work completely changed my
        approach to competition."
      </p>
    </div>

    <div class="result-card">
      ⭐⭐⭐⭐⭐
      <p>
        "The programme fit around my sport and my life."
      </p>
    </div>

  </div>

</section>

<div class="comparison-wrapper">

  <div class="comparison-card mental">

    <div class="comparison-header">
      <span class="comparison-icon">🧠</span>
      <h3>Mental Performance Plan</h3>
    </div>

    <p class="comparison-tagline">
      Build confidence, focus and composure under pressure.
    </p>

    <ul class="comparison-list">
      <li>✓ Confidence building</li>
      <li>✓ Focus & concentration</li>
      <li>✓ Performance anxiety support</li>
      <li>✓ Pre-game routines</li>
      <li>✓ Mental return from injury</li>
      <li>✓ Visualisation techniques</li>
    </ul>

    <div class="comparison-bestfor">
      Best for athletes who feel their mindset is holding back their performance.
    </div>

    <button
      type="button"
      class="consultation-btn"
      id="open-mental-modal"
    >
      Start Mental Plan
    </button>

  </div>

  <div class="comparison-card physical">

    <div class="comparison-header">
      <span class="comparison-icon">💪</span>
      <h3>Physical Performance Plan</h3>
    </div>

    <p class="comparison-tagline">
      Get stronger, more resilient and better prepared for competition.
    </p>

    <ul class="comparison-list">
      <li>✓ Strength & conditioning</li>
      <li>✓ Sport-specific programming</li>
      <li>✓ Injury resilience</li>
      <li>✓ Recovery guidance</li>
      <li>✓ Women's health informed</li>
      <li>✓ Long-term progression</li>
    </ul>

    <div class="comparison-bestfor">
      Best for athletes wanting to improve physical performance and durability.
    </div>

    <button
      type="button"
      class="consultation-btn"
      id="open-consultation-modal"
    >
      Start Physical Plan
    </button>

  </div>

  <div class="comparison-card team">

    <div class="comparison-header">
      <span class="comparison-icon">☘️</span>
      <h3>Team Plan</h3>
    </div>

    <p class="comparison-tagline">
      One squad-wide plan, generated for your team and shared with your players.
    </p>

    <ul class="comparison-list">
      <li>✓ Physical or mental performance</li>
      <li>✓ Built around your training nights and match days</li>
      <li>✓ Reviewed by Emily before going live</li>
      <li>✓ Private link to share with your squad</li>
      <li>✓ No individual player data collected</li>
      <li>✓ Evidence-based, cited sources</li>
    </ul>

    <div class="comparison-bestfor">
      Best for coaches and clubs who want one plan they can run with their whole squad.
    </div>

    <a href="/coach-intake" class="consultation-btn consultation-btn-link">
      Start Team Plan
    </a>

  </div>

</div>

</section>

      <div class="consultation-modal-backdrop" id="consultation-modal-backdrop" aria-hidden="true">
        <div class="consultation-modal" role="dialog" aria-modal="true" aria-labelledby="consultation-modal-title">
          <button type="button" class="consultation-close" id="close-consultation-modal" aria-label="Close consultation form">✕</button>
          <h2 id="consultation-modal-title">Programme Intake Form</h2>
          <p class="consultation-intro">Fill this out and hit submit. Your details come straight to me and I'll be in touch within 48 hours with your plan.</p>

          <form id="consultation-form" class="consultation-form">
            <div class="consultation-grid">
              <label>
                Full Name
                <input type="text" name="name" required />
              </label>

              <label>
                Email
                <input type="email" name="email" required />
              </label>

              <label>
                Phone (optional)
                <input type="tel" name="phone" />
              </label>

              <label>
                Sport / Team
                <input type="text" name="sport" required />
              </label>
            </div>

            <div class="progression-plan-fields">
              <h3>Custom Progression Plan</h3>
              <p class="plan-fields-intro">Optional. Fill in your training details so I can build your programme around them. The more I know, the better the plan.</p>

              <label>
                Does any of the following apply to you? (optional)
                <select name="programTrack">
                  <option value="">Select if applicable</option>
                  <option value="pregnancy">Currently pregnant</option>
                  <option value="postpartum">Postpartum (less than 12 months)</option>
                  <option value="endometriosis">Endometriosis</option>
                  <option value="return_to_play">Returning to play from injury</option>
                </select>
              </label>

              <div class="plan-basic-fields">
                <label>
                  Age
                  <input type="number" name="planAge" min="16" max="100" />
                </label>
                <label>
                  Height (e.g. 5'8 or 173cm)
                  <input type="text" name="planHeight" placeholder="e.g. 5'8 or 173cm" />
                </label>
                <label>
                  Weight (e.g. 65kg, 143lbs)
                  <input type="text" name="planWeight" placeholder="e.g. 65kg, 143lbs" />
                </label>
                <label>
                  Exercise Experience
                  <select name="planExerciseLevel">
                    <option value="">Select level</option>
                    <option value="Beginner (less than 1 year)">Beginner (less than 1 year)</option>
                    <option value="Intermediate (1-3 years)">Intermediate (1-3 years)</option>
                    <option value="Advanced (3+ years)">Advanced (3+ years)</option>
                  </select>
                </label>
                <label>
                  Sports Background
                  <select name="planSportsOrNot">
                    <option value="">Select option</option>
                    <option value="Currently competing">Currently competing in a sport</option>
                    <option value="Previously competed">Previously competed, now fitness</option>
                    <option value="No competitive background">No competitive sports background</option>
                  </select>
                </label>
                <label>
                  Are you actively training in your sport right now?
                  <select name="activeTrainingStatus">
                    <option value="">Select status</option>
                    <option value="actively_competing">Currently training and competing regularly</option>
                    <option value="training_not_competing">Training, but not yet competing</option>
                    <option value="building_back_up">Building back up (returning from break or injury, light training)</option>
                    <option value="not_training">Not currently training with my sport</option>
                  </select>
                </label>
                <label>
                  Current activity level
                  <select name="planActivityLevel">
                    <option value="">Select option</option>
                    <option value="Mostly inactive (less than 1 session per week)">Mostly inactive (less than 1 session per week)</option>
                    <option value="Light (1-2 sessions per week)">Light (1-2 sessions per week)</option>
                    <option value="Moderate (3-4 sessions per week)">Moderate (3-4 sessions per week)</option>
                    <option value="Active (5-6 sessions per week)">Active (5-6 sessions per week)</option>
                    <option value="Highly active (6+ sessions per week)">Highly active (6+ sessions per week)</option>
                  </select>
                </label>
                <label>
                  How many sessions per week can your schedule allow for this plan?
                  <input type="number" name="planFrequency" min="1" max="7" placeholder="e.g. 3" />
                </label>
                <label>
                  How many club or sport trainings do you do per week?
                  <select name="clubTrainingsPerWeek">
                    <option value="">Select</option>
                    <option value="0">None right now</option>
                    <option value="1">1 per week</option>
                    <option value="2">2 per week</option>
                    <option value="3">3 per week</option>
                    <option value="4+">4 or more per week</option>
                  </select>
                </label>
                <label class="plan-equipment-label">
                  Which days do you train with your sport?
                  <div class="plan-checkbox-group training-days-group">
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="mon" /><span>Mon</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="tue" /><span>Tue</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="wed" /><span>Wed</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="thu" /><span>Thu</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="fri" /><span>Fri</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="sat" /><span>Sat</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="sun" /><span>Sun</span></label>
                  </div>
                </label>
                <label>
                  How many matches do you typically play per week?
                  <select name="matchesPerWeek">
                    <option value="">Select</option>
                    <option value="0">None (off-season or pre-season)</option>
                    <option value="1">1 per week</option>
                    <option value="2">2 per week</option>
                    <option value="variable">Variable (championship blocks, etc.)</option>
                  </select>
                </label>
                <label>
                  Plan Duration
                  <select name="planDuration">
                    <option value="">Select duration</option>
                    <option value="6 weeks">6-Week Plan</option>
                    <option value="12 weeks">12-Week Plan</option>
                  </select>
                </label>
                <label>
                  When is this plan for?
                  <select name="seasonPhase">
                    <option value="">Select phase</option>
                    <option value="pre_season">Pre-season (build a base before the season)</option>
                    <option value="championship_leadup">Championship lead-up (peak and taper for matches)</option>
                    <option value="in_season">In-season (maintenance during competition)</option>
                    <option value="off_season">Off-season (rest, recovery, light work)</option>
                  </select>
                </label>
              </div>

              <label class="plan-equipment-label">
                Where will you train?
                <div class="plan-checkbox-group">
                  <label class="checkbox">
                    <input type="checkbox" name="planEquipment" value="Home - bodyweight only" />
                    <span>Home (bodyweight only)</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planEquipment" value="Home - with some equipment" />
                    <span>Home with basic equipment (dumbbells, bands, etc.)</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planEquipment" value="Gym access" />
                    <span>Gym access</span>
                  </label>
                </div>
              </label>

              <label>
                Lifestyle
                <select name="planLifestyle">
                  <option value="">Select option</option>
                  <option value="Office-based, sedentary">Office-based job, mostly sedentary</option>
                  <option value="Active job">Active job (standing, moving)</option>
                  <option value="Outdoor-focused">Outdoor-focused lifestyle</option>
                  <option value="Mix">Mix of sedentary and active</option>
                </select>
              </label>

              <label>
                What does a typical current week look like? (optional)
                <textarea name="currentWeek" rows="2" placeholder="e.g. work Mon-Fri 9-5, training Tue/Thu/Sat, kids on weekends"></textarea>
              </label>

              <label>
                Menstrual cycle status (optional)
                <select name="cycleStatus">
                  <option value="">Select if comfortable</option>
                  <option value="cycling_regularly">Cycling regularly</option>
                  <option value="hormonal_contraception">On hormonal contraception</option>
                  <option value="irregular_or_not_tracking">Irregular or not tracking</option>
                  <option value="pregnant_or_postpartum">Currently pregnant or postpartum</option>
                  <option value="perimenopausal">Perimenopausal or postmenopausal</option>
                  <option value="prefer_not_to_say">Prefer not to say</option>
                </select>
              </label>

              <label>
                Medical Conditions (if any)
                <textarea name="planMedical" rows="2" placeholder="e.g. asthma, diabetes, pcos, none"></textarea>
              </label>

              <label>
                Injuries or Limitations (if any)
                <textarea name="planInjuries" rows="2" placeholder="e.g. lower back, shoulder, none"></textarea>
              </label>

              <label class="plan-goals-label">
                What are your main goals? (select all that apply)
                <div class="plan-checkbox-group">
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="Strength" />
                    <span>Build strength</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="Muscle" />
                    <span>Build muscle</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="Endurance" />
                    <span>Improve endurance</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="Performance" />
                    <span>Sport/athletic performance</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="Recovery" />
                    <span>Injury prevention & recovery</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="planGoals" value="General fitness" />
                    <span>General fitness & health</span>
                  </label>
                </div>
              </label>

              <label class="consultation-full-width">
                Please provide specific details on what you are hoping to achieve
                <textarea name="goals" rows="4" required placeholder="Be as specific as possible about your goals and what success looks like for you"></textarea>
              </label>

              <label>
                Issues, Worries, or Specific Concerns
                <textarea name="planIssues" rows="2" placeholder="e.g. knee pain, recovery, cycle-aware, etc."></textarea>
              </label>

              <label>
                Are you also doing a Mind the Gael Mental Performance Plan? (optional)
                <textarea name="companionPlanSummary" rows="3" placeholder="If yes, paste the highlights or give a short summary (e.g. weekly themes, routines you are building, performance moments you are working on). Your physical plan will be built to reinforce it."></textarea>
              </label>

              <label>
                Anything else we should know? (optional)
                <textarea name="anythingElse" rows="2" placeholder="A catchall. Anything that didn't fit above, or anything you'd want Emily to know before building your plan"></textarea>
              </label>
            </div>

            <div class="intake-disclaimer" role="note">
              <strong>Important.</strong> Mind the Gael creates AI-generated performance coaching, not medical, psychological, or physiotherapy advice. If anything in your plan feels painful, unclear, or unsafe, stop and check with your GP, physio, or a qualified professional. If you are dealing with a mental health crisis, contact the Samaritans on 116 123 (UK and Ireland) or Pieta House on 1800 247 247 (Ireland).
            </div>

            <label class="consultation-consent">
              <input type="checkbox" name="ageConfirm" required />
              <span>I confirm I am 18 years of age or older.</span>
            </label>

            <label class="consultation-consent">
              <input type="checkbox" name="consent" required />
              <span>I consent to being contacted about my consultation request and to my details being used to generate my plan.</span>
            </label>

            <button type="submit" class="consultation-submit">Submit Consultation Request</button>
          </form>
        </div>
      </div>

      <div class="consultation-modal-backdrop" id="mental-modal-backdrop" aria-hidden="true">
        <div class="consultation-modal" role="dialog" aria-modal="true" aria-labelledby="mental-modal-title">
          <button type="button" class="consultation-close" id="close-mental-modal" aria-label="Close mental performance form">✕</button>
          <h2 id="mental-modal-title">Mental Performance Plan Intake</h2>
          <p class="consultation-intro">Tell me about your sport, your competitive history, and the performance moments you want to work on. Your plan is built around what you share here.</p>

          <form id="mental-form" class="consultation-form">
            <div class="consultation-grid">
              <label>
                Full Name
                <input type="text" name="name" required />
              </label>
              <label>
                Email
                <input type="email" name="email" required />
              </label>
              <label>
                Phone (optional)
                <input type="tel" name="phone" />
              </label>
              <label>
                Sport / Team
                <input type="text" name="sport" required />
              </label>
            </div>

            <div class="progression-plan-fields">
              <h3>Your Competitive Background</h3>
              <p class="plan-fields-intro">A bit of context on where you are competing and how long you have been at it. This shapes the level your plan is pitched at.</p>

              <div class="plan-basic-fields">
                <label>
                  Competition Level
                  <select name="competitionLevel" required>
                    <option value="">Select level</option>
                    <option value="Recreational / Local club">Recreational / Local club</option>
                    <option value="Club">Club</option>
                    <option value="County / Regional">County / Regional</option>
                    <option value="Inter-county / National">Inter-county / National</option>
                    <option value="International">International</option>
                    <option value="Other">Other</option>
                  </select>
                </label>
                <label>
                  Years competing in your main sport
                  <select name="yearsCompeting">
                    <option value="">Select</option>
                    <option value="Less than 1 year">Less than 1 year</option>
                    <option value="1-3 years">1-3 years</option>
                    <option value="3-5 years">3-5 years</option>
                    <option value="5-10 years">5-10 years</option>
                    <option value="10+ years">10+ years</option>
                  </select>
                </label>
                <label>
                  Plan Duration
                  <select name="planDuration" required>
                    <option value="">Select duration</option>
                    <option value="6 weeks">6-Week Plan</option>
                    <option value="12 weeks">12-Week Plan</option>
                  </select>
                </label>
                <label>
                  When is this plan for?
                  <select name="seasonPhase">
                    <option value="">Select phase</option>
                    <option value="pre_season">Pre-season (build a base before the season)</option>
                    <option value="championship_leadup">Championship lead-up (peak and taper for matches)</option>
                    <option value="in_season">In-season (maintenance during competition)</option>
                    <option value="off_season">Off-season (rest, reflection, mental skills work)</option>
                  </select>
                </label>
                <label>
                  Are you actively training in your sport right now?
                  <select name="activeTrainingStatus">
                    <option value="">Select status</option>
                    <option value="actively_competing">Currently training and competing regularly</option>
                    <option value="training_not_competing">Training, but not yet competing</option>
                    <option value="building_back_up">Building back up (returning from break or injury, light training)</option>
                    <option value="not_training">Not currently training with my sport</option>
                  </select>
                </label>
                <label>
                  How many club or sport trainings do you do per week?
                  <select name="clubTrainingsPerWeek">
                    <option value="">Select</option>
                    <option value="0">None right now</option>
                    <option value="1">1 per week</option>
                    <option value="2">2 per week</option>
                    <option value="3">3 per week</option>
                    <option value="4+">4 or more per week</option>
                  </select>
                </label>
                <label class="plan-equipment-label">
                  Which days do you train with your sport?
                  <div class="plan-checkbox-group training-days-group">
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="mon" /><span>Mon</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="tue" /><span>Tue</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="wed" /><span>Wed</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="thu" /><span>Thu</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="fri" /><span>Fri</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="sat" /><span>Sat</span></label>
                    <label class="checkbox"><input type="checkbox" name="trainingDays" value="sun" /><span>Sun</span></label>
                  </div>
                </label>
                <label>
                  How many matches do you typically play per week?
                  <select name="matchesPerWeek">
                    <option value="">Select</option>
                    <option value="0">None right now</option>
                    <option value="1">1 per week</option>
                    <option value="2">2 per week</option>
                    <option value="variable">Variable (championship blocks, etc.)</option>
                  </select>
                </label>
                <label>
                  Current confidence level (1-10)
                  <select name="confidenceLevel">
                    <option value="">Select</option>
                    <option value="1">1 - very low</option>
                    <option value="2">2</option>
                    <option value="3">3</option>
                    <option value="4">4</option>
                    <option value="5">5 - middle</option>
                    <option value="6">6</option>
                    <option value="7">7</option>
                    <option value="8">8</option>
                    <option value="9">9</option>
                    <option value="10">10 - very high</option>
                  </select>
                </label>
              </div>

              <label class="plan-goals-label">
                Which performance moments do you want to work on? (select all that apply)
                <div class="plan-checkbox-group">
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Pre-game nerves" />
                    <span>Pre-game nerves and pressure</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Confidence" />
                    <span>Building and holding confidence</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Focus during play" />
                    <span>Focus and concentration during play</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Recovery from mistakes" />
                    <span>Recovering from mistakes mid-game</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Pre-performance routines" />
                    <span>Pre-performance routines</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Self-talk" />
                    <span>Self-talk under fatigue</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Visualisation" />
                    <span>Visualisation and imagery</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Motivation" />
                    <span>Motivation and goal setting</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Team and social pressure" />
                    <span>Team and social pressure</span>
                  </label>
                  <label class="checkbox">
                    <input type="checkbox" name="performanceMoments" value="Return from injury" />
                    <span>Mental return from injury</span>
                  </label>
                </div>
              </label>

              <label>
                Any current mental performance routines you already use? (optional)
                <textarea name="currentRoutines" rows="2" placeholder="e.g. breathing before warm-up, visualisation the night before, a phrase you say to yourself, journaling. Or none."></textarea>
              </label>

              <label>
                A peak moment you've had in your sport (optional)
                <textarea name="peakMoment" rows="3" placeholder="A moment you felt fully on top of things. What was happening, what did it feel like, what were you doing differently?"></textarea>
              </label>

              <label>
                A struggle moment you've had in your sport (optional)
                <textarea name="struggleMoment" rows="3" placeholder="A moment things felt off. What happened, what was running through your head, what would you want to change about that response?"></textarea>
              </label>

              <label class="consultation-full-width">
                What do you most want this plan to help you with?
                <textarea name="goals" rows="4" required placeholder="Be specific. What does success look like for you by the end of this plan?"></textarea>
              </label>

              <label>
                Are you also doing a Mind the Gael Physical Training Plan? (optional)
                <textarea name="companionPlanSummary" rows="3" placeholder="If yes, paste the highlights or give a short summary (e.g. session split, focus areas, weeks completed). Your mental plan will be built to apply directly to those training and match moments."></textarea>
              </label>

              <label>
                Anything else I should know? (optional)
                <textarea name="anythingElse" rows="2" placeholder="A catchall. Anything that didn't fit above."></textarea>
              </label>
            </div>

            <div class="intake-disclaimer" role="note">
              <strong>Important.</strong> Mind the Gael creates AI-generated performance coaching, not medical, psychological, or therapeutic advice. If anything in your plan feels off or you are not in a safe place mentally, stop and reach out for proper support. Samaritans 116 123 (UK and Ireland), Pieta House 1800 247 247 (Ireland), or your GP.
            </div>

            <label class="consultation-consent">
              <input type="checkbox" name="ageConfirm" required />
              <span>I confirm I am 18 years of age or older.</span>
            </label>

            <label class="consultation-consent">
              <input type="checkbox" name="consent" required />
              <span>I consent to being contacted about my mental performance plan and to my details being used to generate my plan.</span>
            </label>

            <button type="submit" class="consultation-submit">Submit Mental Performance Intake</button>
          </form>
        </div>
      </div>

      <div class="intake-loading-overlay" id="intake-loading-overlay" aria-hidden="true" role="status">
        <div class="intake-loading-card">
          <div class="intake-loading-spinner" aria-hidden="true"></div>
          <p class="intake-loading-title">Building your preview</p>
          <p class="intake-loading-status" id="intake-loading-status">Reading your details...</p>
          <p class="intake-loading-footnote">This usually takes a few seconds.</p>
        </div>
      </div>
    </main>
    <Footer />

    <script is:inline>
      // ── Shared intake-submit loading overlay ──
      // Both the mental and physical intake forms route through Anthropic
      // for a teaser. The wait is short (~2-3s on Haiku) but feels long if
      // the page looks frozen. The overlay rotates short status messages
      // so the wait feels intentional.
      const intakeLoadingOverlay = document.getElementById('intake-loading-overlay');
      const intakeLoadingStatus = document.getElementById('intake-loading-status');
      const INTAKE_LOADING_STEPS = [
        'Reading your details...',
        'Picking your weekly themes...',
        'Building your preview...',
        'Almost there...',
      ];
      let intakeLoadingTimer = null;

      const startIntakeLoading = () => {
        if (!intakeLoadingOverlay) return;
        intakeLoadingOverlay.classList.add('active');
        intakeLoadingOverlay.setAttribute('aria-hidden', 'false');
        let step = 0;
        if (intakeLoadingStatus) intakeLoadingStatus.textContent = INTAKE_LOADING_STEPS[0];
        intakeLoadingTimer = window.setInterval(() => {
          step = Math.min(step + 1, INTAKE_LOADING_STEPS.length - 1);
          if (intakeLoadingStatus) intakeLoadingStatus.textContent = INTAKE_LOADING_STEPS[step];
        }, 1800);
      };

      const stopIntakeLoading = () => {
        if (!intakeLoadingOverlay) return;
        intakeLoadingOverlay.classList.remove('active');
        intakeLoadingOverlay.setAttribute('aria-hidden', 'true');
        if (intakeLoadingTimer) {
          window.clearInterval(intakeLoadingTimer);
          intakeLoadingTimer = null;
        }
      };

      const openModalBtn = document.getElementById('open-consultation-modal');
      const closeModalBtn = document.getElementById('close-consultation-modal');
      const modalBackdrop = document.getElementById('consultation-modal-backdrop');
      const consultationForm = document.getElementById('consultation-form');

      const openModal = () => {
        if (!modalBackdrop) return;
        modalBackdrop.classList.add('active');
        modalBackdrop.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
      };

      const closeModal = () => {
        if (!modalBackdrop) return;
        modalBackdrop.classList.remove('active');
        modalBackdrop.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
      };

      if (openModalBtn) {
        openModalBtn.addEventListener('click', openModal);
      }

      if (closeModalBtn) {
        closeModalBtn.addEventListener('click', closeModal);
      }

      if (modalBackdrop) {
        modalBackdrop.addEventListener('click', (event) => {
          if (event.target === modalBackdrop) {
            closeModal();
          }
        });
      }

      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') {
          closeModal();
        }
      });

      if (consultationForm instanceof HTMLFormElement) {
        consultationForm.addEventListener('submit', async (event) => {
          event.preventDefault();

          const submitBtn = consultationForm.querySelector('.consultation-submit');
          const originalSubmitLabel = submitBtn ? submitBtn.textContent : '';
          if (submitBtn instanceof HTMLButtonElement) {
            submitBtn.disabled = true;
            submitBtn.textContent = 'Submitting...';
          }
          startIntakeLoading();

          const formData = new FormData(consultationForm);
          const name = String(formData.get('name') || '').trim();
          const email = String(formData.get('email') || '').trim();
          const phone = String(formData.get('phone') || '').trim();
          const sport = String(formData.get('sport') || '').trim();
          const goals = String(formData.get('goals') || '').trim();

          // Build request payload
          const requestData = {
            name,
            email,
            phone: phone || undefined,
            sport,
            goals,
            includeProgressionPlan: true,
          };

          // Add progression plan fields
          const planAge = String(formData.get('planAge') || '').trim();
          const planFrequency = String(formData.get('planFrequency') || '').trim();
          const planGoalsArray = [];
          
          // Get all planGoals checkboxes
          formData.getAll('planGoals').forEach(goal => {
            planGoalsArray.push(String(goal));
          });

          const equipmentArray = [];
          
          // Get all planEquipment checkboxes
          formData.getAll('planEquipment').forEach(equip => {
            equipmentArray.push(String(equip));
          });

          Object.assign(requestData, {
            age: planAge ? parseInt(planAge) : undefined,
            height: String(formData.get('planHeight') || '').trim() || undefined,
            weight: String(formData.get('planWeight') || '').trim() || undefined,
            exerciseLevel: String(formData.get('planExerciseLevel') || '').trim() || undefined,
            sportsOrNot: String(formData.get('planSportsOrNot') || '').trim() || undefined,
            equipment: equipmentArray,
            frequencyPerWeek: planFrequency ? parseInt(planFrequency) : undefined,
            planDuration: String(formData.get('planDuration') || '').trim() || undefined,
            planGoals: planGoalsArray,
            issuesWorries: String(formData.get('planIssues') || '').trim() || undefined,
            lifestyle: String(formData.get('planLifestyle') || '').trim() || undefined,
            medicalConditions: String(formData.get('planMedical') || '').trim() || undefined,
            injuries: String(formData.get('planInjuries') || '').trim() || undefined,
            programTrack: String(formData.get('programTrack') || '').trim() || undefined,
            cycleStatus: String(formData.get('cycleStatus') || '').trim() || undefined,
            currentWeek: String(formData.get('currentWeek') || '').trim() || undefined,
            anythingElse: String(formData.get('anythingElse') || '').trim() || undefined,
            currentActivityLevel: String(formData.get('planActivityLevel') || '').trim() || undefined,
            companionPlanSummary: String(formData.get('companionPlanSummary') || '').trim() || undefined,
            seasonPhase: String(formData.get('seasonPhase') || '').trim() || undefined,
            clubTrainingsPerWeek: String(formData.get('clubTrainingsPerWeek') || '').trim() || undefined,
            matchesPerWeek: String(formData.get('matchesPerWeek') || '').trim() || undefined,
            trainingDays: formData.getAll('trainingDays').map(String).filter(Boolean),
            activeTrainingStatus: String(formData.get('activeTrainingStatus') || '').trim() || undefined,
          });

          // Submit to the new preview-then-buy endpoint. On success we redirect
          // the user to /programme-preview where they see a teaser and can buy.
          try {
            const response = await fetch('/api/programme-intake', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(requestData),
            });

            const result = await response.json().catch(() => ({}));
            if (!response.ok && result.reason !== 'red_flag') {
              throw new Error(result.message || 'Submission failed.');
            }

            if (result.success && result.previewToken) {
              closeModal();
              consultationForm.reset();
              window.location.href = '/programme-preview?token=' + encodeURIComponent(result.previewToken);
              return;
            }

            if (result.reason === 'red_flag' && result.message) {
              // Red-flag case: no preview, no purchase. Show Emily's support message.
              closeModal();
              consultationForm.reset();
              alert(result.message);
              return;
            }

            alert('Error: ' + (result.message || 'Something went wrong'));
          } catch (error) {
            console.error('Error:', error);
            const message = error instanceof Error
              ? error.message
              : 'Failed to submit your request. Please try again or contact me directly at emilyphelan@mindthegael.co.uk';
            alert(message);
          } finally {
            if (submitBtn instanceof HTMLButtonElement) {
              submitBtn.disabled = false;
              submitBtn.textContent = originalSubmitLabel || 'Submit Consultation Request';
            }
            stopIntakeLoading();
          }
        });
      }

      // ── Mental Performance Plan modal + form ──

      const openMentalBtn = document.getElementById('open-mental-modal');
      const closeMentalBtn = document.getElementById('close-mental-modal');
      const mentalBackdrop = document.getElementById('mental-modal-backdrop');
      const mentalForm = document.getElementById('mental-form');

      const openMental = () => {
        if (!mentalBackdrop) return;
        mentalBackdrop.classList.add('active');
        mentalBackdrop.setAttribute('aria-hidden', 'false');
        document.body.style.overflow = 'hidden';
      };

      const closeMental = () => {
        if (!mentalBackdrop) return;
        mentalBackdrop.classList.remove('active');
        mentalBackdrop.setAttribute('aria-hidden', 'true');
        document.body.style.overflow = '';
      };

      if (openMentalBtn) openMentalBtn.addEventListener('click', openMental);
      if (closeMentalBtn) closeMentalBtn.addEventListener('click', closeMental);
      if (mentalBackdrop) {
        mentalBackdrop.addEventListener('click', (event) => {
          if (event.target === mentalBackdrop) closeMental();
        });
      }
      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') closeMental();
      });

      if (mentalForm instanceof HTMLFormElement) {
        mentalForm.addEventListener('submit', async (event) => {
          event.preventDefault();

          const submitBtn = mentalForm.querySelector('.consultation-submit');
          const originalSubmitLabel = submitBtn ? submitBtn.textContent : '';
          if (submitBtn instanceof HTMLButtonElement) {
            submitBtn.disabled = true;
            submitBtn.textContent = 'Submitting...';
          }
          startIntakeLoading();

          const formData = new FormData(mentalForm);

          const performanceMoments = [];
          formData.getAll('performanceMoments').forEach((m) => {
            performanceMoments.push(String(m));
          });

          const requestData = {
            planType: 'mental',
            name: String(formData.get('name') || '').trim(),
            email: String(formData.get('email') || '').trim(),
            phone: String(formData.get('phone') || '').trim() || undefined,
            sport: String(formData.get('sport') || '').trim(),
            goals: String(formData.get('goals') || '').trim(),
            includeProgressionPlan: true,
            planDuration: String(formData.get('planDuration') || '').trim() || undefined,
            competitionLevel: String(formData.get('competitionLevel') || '').trim() || undefined,
            yearsCompeting: String(formData.get('yearsCompeting') || '').trim() || undefined,
            confidenceLevel: String(formData.get('confidenceLevel') || '').trim() || undefined,
            performanceMomentsToWorkOn: performanceMoments,
            currentRoutines: String(formData.get('currentRoutines') || '').trim() || undefined,
            peakMoment: String(formData.get('peakMoment') || '').trim() || undefined,
            struggleMoment: String(formData.get('struggleMoment') || '').trim() || undefined,
            companionPlanSummary: String(formData.get('companionPlanSummary') || '').trim() || undefined,
            seasonPhase: String(formData.get('seasonPhase') || '').trim() || undefined,
            clubTrainingsPerWeek: String(formData.get('clubTrainingsPerWeek') || '').trim() || undefined,
            matchesPerWeek: String(formData.get('matchesPerWeek') || '').trim() || undefined,
            trainingDays: formData.getAll('trainingDays').map(String).filter(Boolean),
            activeTrainingStatus: String(formData.get('activeTrainingStatus') || '').trim() || undefined,
            anythingElse: String(formData.get('anythingElse') || '').trim() || undefined,
          };

          try {
            const response = await fetch('/api/programme-intake', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(requestData),
            });

            const result = await response.json().catch(() => ({}));
            if (!response.ok && result.reason !== 'red_flag') {
              throw new Error(result.message || 'Submission failed.');
            }

            if (result.success && result.previewToken) {
              closeMental();
              mentalForm.reset();
              window.location.href = '/programme-preview?token=' + encodeURIComponent(result.previewToken);
              return;
            }

            if (result.reason === 'red_flag' && result.message) {
              closeMental();
              mentalForm.reset();
              alert(result.message);
              return;
            }

            alert('Error: ' + (result.message || 'Something went wrong'));
          } catch (error) {
            console.error('Error:', error);
            const message = error instanceof Error
              ? error.message
              : 'Failed to submit your request. Please try again or contact me directly at emilyphelan@mindthegael.co.uk';
            alert(message);
          } finally {
            if (submitBtn instanceof HTMLButtonElement) {
              submitBtn.disabled = false;
              submitBtn.textContent = originalSubmitLabel || 'Submit Mental Performance Intake';
            }
            stopIntakeLoading();
          }
        });
      }
    </script>
  </body>
</html>

<style>
  .personal-training-page main.container {
    padding-top: 0;
    padding-bottom: 0.75rem;
  }

  .pt-band {
    margin: 0 calc(50% - 50vw);
    padding: 1.5rem max(1rem, calc(50vw - 600px));
  }

  .pt-band-intro {
    background: linear-gradient(180deg, rgba(3, 10, 6, 0.95) 0%, rgba(3, 10, 6, 0.82) 100%);
  }

  .pt-band-focus {
    background: linear-gradient(180deg, rgba(3, 10, 6, 0.98) 0%, rgba(3, 10, 6, 0.9) 100%);
    padding: 0;
  }

  .pt-band-focus .pt-band-inner {
    max-width: 1380px;
  }

  .pt-band-pricing {
    background: linear-gradient(180deg, rgba(3, 10, 6, 0.95) 0%, rgba(3, 10, 6, 0.82) 100%);
  }

  .pt-band-mental {
    background: #ffffff;
  }

  .pt-band-focus-areas {
    background: #ffffff;
  }

  .pt-band-booking {
    background: linear-gradient(180deg, rgba(3, 10, 6, 0.95) 0%, rgba(3, 10, 6, 0.82) 100%);
  }

  .pt-band-inner {
    max-width: 1200px;
    margin: 0 auto;
  }

  h1 {
    color: var(--green-lime);
    font-size: 2rem;
    margin: 1rem 0;
    padding: 0 0.2rem;
  }

  .intro p {
    font-size: 1.1rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .pt-band-focus p,
  .pt-band-focus .training-highlight-copy p {
    color: var(--green-lime);
  }

  .pt-band-focus .training-highlight-copy h2 {
    color: var(--green-lime);
  }

  .pt-band-focus-areas p,
  .pt-band-focus-areas .focus-card p,
  .pt-band-booking p,
  .pt-band-booking .calendar-copy,
  .pt-band-booking .consultation-note {
    color: var(--green-light);
  }

  .pt-band-focus-areas p,
  .pt-band-focus-areas .focus-card p {
    color: var(--green-dark);
  }

  .pt-band-mental .pricing-heading {
    color: var(--purple);
  }

  .pt-band-mental .pricing-intro,
  .pt-band-mental .plan-option-item h3,
  .pt-band-mental .plan-option-item p {
    color: var(--green-dark);
  }

  .pt-band-mental .plan-option-item {
    background: var(--green-dark);
    border-color: rgba(192, 254, 113, 0.45);
  }

  .pt-band-mental .plan-option-item h3 {
    color: var(--green-lime);
  }

  .pt-band-mental .plan-option-item p {
    color: var(--green-light);
  }

  .pt-band-mental .plan-option-price {
    color: var(--green-lime) !important;
  }

  .pt-band-mental .plan-option-item h4 {
    color: var(--green-lime);
  }

  .pt-band-mental .plan-option-list-bullets,
  .pt-band-mental .plan-option-list-bullets li {
    color: var(--green-light);
  }

  .pt-band-mental .plan-option-best-for {
    color: var(--green-light);
    border-top-color: rgba(192, 254, 113, 0.35);
  }

  .pt-band-mental .plan-option-best-for strong {
    color: var(--green-lime);
  }

  .pt-band-mental .start-plan-heading {
    margin: 1.5rem 0 0.45rem;
    color: var(--purple);
    font-size: 1.22rem;
  }

  .pt-band-mental .consultation-note {
    margin-top: 0;
    margin-bottom: 0.85rem;
    max-width: 64ch;
    color: var(--green-dark);
  }

  .pt-band-mental #open-mental-modal {
    margin-top: 1rem;
  }

  .pt-band-booking .calendar-heading {
    color: var(--green-lime);
  }

  .pt-band-focus-areas .focus-card h3 {
    color: var(--purple);
  }

  .pt-band-focus-areas .focus-card p {
    color: var(--green-dark);
  }

  .pt-band-booking .calendar-copy,
  .pt-band-booking .consultation-note {
    color: var(--green-light);
  }

  .pricing-heading {
    margin: 0 0 0.4rem;
    color: var(--green-lime);
  }

  .pricing-intro {
    margin: 0 0 0.9rem;
    color: var(--green-light);
    max-width: 64ch;
    line-height: 1.5;
  }

  .plan-option-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 1rem;
  }

  .plan-option-item {
    background: #ffffff;
    border: 1px solid rgba(105, 0, 90, 0.25);
    border-radius: 12px;
    padding: 1rem;
    box-shadow: 0 8px 18px rgba(0, 0, 0, 0.12);
  }

  .plan-option-item h3 {
    margin: 0 0 0.5rem;
    color: var(--purple);
    font-size: 1.1rem;
  }

  .plan-option-item p {
    margin: 0;
    color: var(--green-dark);
    line-height: 1.55;
  }

  .plan-option-item p + p,
  .plan-option-item p + ul,
  .plan-option-item h4 + ul {
    margin-top: 0.6rem;
  }

  .plan-option-price {
    font-weight: 700;
    font-size: 1.05rem;
    color: var(--green-dark) !important;
    margin: 0 0 0.6rem !important;
    letter-spacing: 0.02em;
  }

  .plan-option-item h4 {
    margin: 1rem 0 0.4rem;
    font-size: 0.92rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--purple);
  }

  .plan-option-list-bullets {
    margin: 0 0 0.6rem;
    padding-left: 1.1rem;
    color: var(--green-dark);
    line-height: 1.55;
  }

  .plan-option-list-bullets li {
    margin-bottom: 0.35rem;
  }

  .plan-option-best-for {
    margin-top: 0.8rem !important;
    padding-top: 0.7rem;
    border-top: 1px solid rgba(105, 0, 90, 0.18);
    font-size: 0.95rem;
    color: var(--green-dark);
  }

  .pricing-table-wrap {
    overflow-x: auto;
    margin-bottom: 0;
  }

  .pricing-table {
    width: 100%;
    border-collapse: collapse;
    background-color: rgba(244, 255, 232, 0.95);
    border: 2px solid rgba(105, 0, 90, 0.5);
    color: var(--green-dark);
  }

  .pricing-table caption {
    caption-side: top;
    text-align: left;
    color: rgba(244, 255, 232, 0.95);
    font-size: 0.95rem;
    margin-bottom: 0.5rem;
  }

  .pricing-table th,
  .pricing-table td {
    padding: 0.85rem;
    text-align: left;
    border: 1px solid rgba(105, 0, 90, 0.45);
    vertical-align: middle;
  }

  .pricing-table th {
    background: rgba(192, 254, 113, 0.14);
    color: var(--purple);
    font-weight: 700;
  }

  .pricing-cta {
    display: inline-block;
    margin-top: 0.6rem;
    padding: 0.55rem 0.9rem;
    border-radius: 8px;
    background: var(--green-lime);
    color: var(--green-dark);
    font-weight: 700;
    border: 2px solid var(--green-lime);
    transition: transform 0.15s ease, border-color 0.15s ease, color 0.15s ease;
  }

  .pricing-cta:hover {
    transform: translateY(-1px);
    border-color: var(--purple-light);
    color: var(--purple);
  }

  .calendar-section {
    margin: 0;
  }

  .calendar-heading {
    margin: 0 0 0.9rem;
    color: var(--green-lime);
  }

  .calendar-copy {
    margin: 0 0 1rem;
  }

  .consultation-note {
    margin: 0 0 1rem;
    font-size: 0.92rem;
    color: var(--green-dark);
  }

  .consultation-btn-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin: 0 0 0.5rem;
  }

  .consultation-btn {
    display: inline-block;
    margin: 0 0 1rem;
    padding: 0.7rem 1.1rem;
    border-radius: 8px;
    border: 2px solid transparent;
    background: var(--green-lime);
    color: var(--green-dark);
    font-weight: 700;
    font: inherit;
    line-height: 1.2;
    cursor: pointer;
    transition: background-color 0.2s ease, border-color 0.2s ease;
  }

  .consultation-btn-row .consultation-btn {
    margin: 0;
  }

  .consultation-btn:hover {
    background: var(--purple-light);
    border-color: var(--purple);
  }

  .calendar-embed-wrap {
    width: 100%;
    border: 1px solid rgba(192, 254, 113, 0.45);
    border-radius: 16px;
    overflow: hidden;
    background: #fff;
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.14);
  }

  .calendar-embed-wrap iframe {
    width: 100%;
    height: 880px;
    border: 0;
    display: block;
    background: #fff;
  }

  .focus-areas {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin-bottom: 0;
    padding-top: 0.25rem;
  }

  .training-highlight {
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 0;
    margin: 0;
    min-height: 380px;
    border: 0;
    border-radius: 0;
    overflow: hidden;
    background: transparent;
  }

  .training-highlight-copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 2rem 1rem 2rem max(1rem, calc((100vw - 1200px) / 2 - (100vw - min(1380px, 100vw)) / 2));
  }

  .training-highlight-copy h2 {
    margin: 0 0 0.55rem;
    color: var(--purple);
    font-size: 1.4rem;
  }

  .training-highlight-copy p {
    margin: 0;
    color: var(--green-dark);
  }

  .training-highlight-media {
    position: relative;
    align-self: stretch;
    min-height: 240px;
  }

  .training-highlight-media :global(img) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 58% 35%;
    border-radius: 0;
    display: block;
  }

  @media (max-width: 768px) {
    .pt-band {
      padding: 1.1rem max(0.8rem, calc(50vw - 600px));
    }

    .pt-band-focus {
      padding-top: 0;
      padding-bottom: 0;
    }

    .waitlist-form {
      grid-template-columns: 1fr;
      max-width: 100%;
    }

    .training-highlight {
      grid-template-columns: 1fr;
      min-height: 0;
    }

    .training-highlight-copy {
      padding: 1.2rem 1rem;
    }

    .training-highlight-media {
      min-height: 190px;
      order: -1;
      margin-top: 0;
    }

    .focus-areas {
      grid-template-columns: 1fr;
    }

    .plan-option-list {
      grid-template-columns: 1fr;
    }

    .calendar-embed-wrap iframe {
      height: 760px;
    }
  }

  @media (max-width: 520px) {
    .calendar-embed-wrap {
      border-radius: 12px;
    }

    .calendar-embed-wrap iframe {
      height: 700px;
    }
  }

  .focus-card {
    padding: 1.2rem 0.2rem;
    background-color: transparent;
    border: 0;
    border-radius: 0;
    transition: transform 0.2s ease, border-color 0.2s ease;
  }

  .focus-card:hover {
    transform: none;
    border-color: transparent;
  }

  .focus-card h3 {
    color: var(--purple);
    font-size: 1.2rem;
    margin: 0 0 0.5rem 0;
  }

  .focus-card p {
    color: var(--green-dark);
  }

  .coming-soon-strip {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto 1rem;
    border-radius: 8px;
    background: #f0cdff;
    color: #69005a;
    border: 2px solid #69005a;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    padding: 0.65rem 1rem;
    font-weight: 700;
    font-size: 0.95rem;
    text-align: center;
  }

  .strip-shamrock {
    width: 28px;
    height: 28px;
    flex-shrink: 0;
  }

  .consultation-modal-backdrop {
    position: fixed !important;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.6);
    display: none;
    justify-content: center;
    padding: 2rem 1rem;
    z-index: 3000;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }

  .intake-loading-overlay {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(3, 10, 6, 0.78);
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 4000;
    padding: 1.5rem;
  }

  .intake-loading-overlay.active {
    display: flex;
  }

  .intake-loading-card {
    background: var(--green-dark);
    color: var(--green-light);
    border: 2px solid var(--green-lime);
    border-radius: 16px;
    padding: 2.2rem 2rem;
    max-width: 440px;
    width: 100%;
    text-align: center;
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.45);
  }

  .intake-loading-spinner {
    width: 56px;
    height: 56px;
    border-radius: 50%;
    border: 4px solid rgba(192, 254, 113, 0.25);
    border-top-color: var(--green-lime);
    margin: 0 auto 1.1rem;
    animation: intake-spin 0.9s linear infinite;
  }

  .intake-loading-title {
    margin: 0 0 0.4rem;
    color: var(--green-lime);
    font-size: 1.25rem;
    font-weight: 700;
  }

  .intake-loading-status {
    margin: 0 0 0.9rem;
    color: var(--green-light);
    font-size: 1rem;
    min-height: 1.5em;
    transition: opacity 0.2s ease;
  }

  .intake-loading-footnote {
    margin: 0;
    color: rgba(244, 255, 232, 0.7);
    font-size: 0.88rem;
  }

  @keyframes intake-spin {
    to { transform: rotate(360deg); }
  }

  .consultation-modal-backdrop.active {
    display: flex;
    align-items: flex-start;
  }

  .consultation-modal {
    width: min(760px, 100%);
    border: 2px solid var(--green-lime);
    border-radius: 12px;
    background: var(--green-dark);
    padding: 1.1rem;
    position: relative;
    margin: auto;
  }

  .consultation-close {
    position: absolute;
    top: 0.65rem;
    right: 0.65rem;
    width: 2rem;
    height: 2rem;
    border: 0;
    border-radius: 6px;
    background: rgba(240, 205, 255, 0.22);
    color: var(--green-light);
    cursor: pointer;
  }

  .consultation-modal h2 {
    margin: 0;
    color: var(--green-lime);
    font-size: 1.25rem;
  }

  .consultation-intro {
    margin: 0.55rem 0 1rem;
    color: var(--green-light);
  }

  .consultation-form {
    display: grid;
    gap: 1rem;
  }

  .consultation-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.85rem;
  }

  .consultation-form label {
    display: grid;
    gap: 0.4rem;
    border: 1px solid rgba(192, 254, 113, 0.26);
    border-radius: 10px;
    padding: 0.65rem 0.7rem;
    background: rgba(3, 10, 6, 0.34);
    color: var(--green-light);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .consultation-form input,
  .consultation-form select,
  .consultation-form textarea {
    width: 100%;
    border-radius: 8px;
    border: 1px solid rgba(105, 0, 90, 0.35);
    background: rgba(255, 255, 255, 0.92);
    color: #121212;
    padding: 0.64rem 0.72rem;
    font: inherit;
  }

  .consultation-form textarea {
    min-height: 120px;
    resize: vertical;
  }

  .consultation-full-width {
    grid-column: span 2;
  }

  .intake-disclaimer {
    margin: 0.4rem 0 1rem;
    padding: 0.85rem 1rem;
    border-radius: 8px;
    background: rgba(240, 205, 255, 0.12);
    border-left: 4px solid var(--green-lime);
    color: var(--green-light);
    font-size: 0.88rem;
    line-height: 1.5;
  }

  .intake-disclaimer strong {
    color: var(--green-lime);
  }

  .consultation-consent {
    display: flex !important;
    align-items: center;
    gap: 0.55rem;
    border: 1px solid rgba(192, 254, 113, 0.26);
    border-radius: 10px;
    padding: 0.65rem 0.7rem;
    background: rgba(3, 10, 6, 0.34);
    font-weight: 500 !important;
  }

  .consultation-consent input {
    width: auto;
    margin: 0;
  }

  .consultation-submit {
    justify-self: start;
    padding: 0.7rem 1.1rem;
    border-radius: 6px;
    border: 2px solid transparent;
    background: var(--green-lime);
    color: var(--green-dark);
    font-weight: 700;
    font: inherit;
    line-height: 1.2;
    cursor: pointer;
    transition: opacity 0.15s ease;
  }

  .consultation-submit:disabled {
    opacity: 0.7;
    cursor: not-allowed;
  }

  /* Progression Plan Form Styles */
  .progression-plan-fields {
    border: 1px solid rgba(192, 254, 113, 0.2);
    border-radius: 10px;
    padding: 1rem;
    background: rgba(192, 254, 113, 0.06);
    margin: 1rem 0;
  }

  .progression-plan-fields h3 {
    color: var(--green-lime);
    font-size: 1rem;
    margin: 0 0 0.6rem;
  }

  .plan-fields-intro {
    margin: 0 0 0.8rem;
    color: var(--green-light);
    font-size: 0.9rem;
    font-weight: 500;
  }

  .plan-basic-fields {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 0.6rem;
    margin-bottom: 0.8rem;
  }

  .plan-basic-fields label {
    display: grid;
    gap: 0.3rem;
    color: var(--green-light);
    font-weight: 600;
    font-size: 0.85rem;
  }

  .plan-basic-fields input,
  .plan-basic-fields select {
    width: 100%;
    border-radius: 6px;
    border: 1px solid rgba(105, 0, 90, 0.3);
    background: rgba(255, 255, 255, 0.85);
    color: #121212;
    padding: 0.4rem 0.5rem;
    font: inherit;
    font-size: 0.85rem;
  }

  .plan-equipment-label,
  .plan-goals-label {
    display: grid;
    gap: 0.5rem;
  }

  .plan-checkbox-group {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 0.5rem;
  }

  .plan-checkpoint-group .checkbox {
    padding: 0.4rem 0.5rem;
    font-size: 0.9rem;
    border-radius: 4px;
  }

  .progression-plan-fields > label {
    display: grid;
    gap: 0.4rem;
    margin-bottom: 0.6rem;
    color: var(--green-light);
    font-weight: 600;
    font-size: 0.85rem;
  }

  .progression-plan-fields textarea,
  .progression-plan-fields select {
    width: 100%;
    border-radius: 6px;
    border: 1px solid rgba(105, 0, 90, 0.3);
    background: rgba(255, 255, 255, 0.85);
    color: #121212;
    padding: 0.5rem;
    font: inherit;
    font-size: 0.85rem;
  }

  .progression-plan-fields textarea {
    min-height: 60px;
    resize: vertical;
  }

</style>